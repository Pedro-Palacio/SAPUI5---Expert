<mvc:View controllerName="logaligroup.SAPUI5.controller.InvoicesList" xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">
    <List id="idInvoiceList" headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" items="{invoice>/Invoices}">
        <items>
            <ObjectListItem id="idObjectListItem" title="{invoice>Quantity} x {invoice>ProductName}" 
            number="{
			parts: [{path: 'invoice>ExtendedPrice'}, {path: 'currency>/eur'}],
			type: 'sap.ui.model.type.Currency',
			formatOptions: {
				showMeasure: false
			}
		}"  numberUnit="{currency>/eur}" 
        numberState="{=${invoice>ExtendedPrice} > 50 ? 'Warning' : 'Error'}"/>
        </items>
    </List>
</mvc:View>
